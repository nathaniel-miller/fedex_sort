<%= form_for(sort_type) do |f| %>
  <% if sort_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sort_type.errors.count, "error") %> prohibited this sort_type from being saved:</h2>

      <ul>
      <% sort_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
    <%= f.label :setup %><br />
    <%= f.collection_select :setup_id, Setup.all, :id, :name %>
    <%= f.label :roster %><br />
    <%= f.collection_select :roster_id, Roster.all, :id, :name %>
    <%= f.submit %>
  </div>
<% end %>
