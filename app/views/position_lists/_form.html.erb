<%= form_for(position_list) do |f| %>
  <% if position_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(position_list.errors.count, "error") %> prohibited this position_list from being saved:</h2>

      <ul>
      <% position_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
